# Load Testing & Performance Analysis

Этот набор инструментов предоставляет комплексное решение для нагрузочного тестирования и анализа производительности API supreme-octo-succotash.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Скрипты тестирования](#скрипты-тестирования)
3. [Анализ результатов](#анализ-результатов)
4. [Профилирование](#профилирование)
5. [Рекомендации по оптимизации](#рекомендации-по-оптимизации)

## 🚀 Быстрый старт

### 1. Запуск сервера
```bash
python main_clean.py
```

### 2. Проверка работоспособности
```bash
python test_remaining_endpoints.py
```

### 3. Запуск базового нагрузочного теста
```bash
python load_test_with_profiling.py
```

### 4. Запуск комплексного стресс-тестирования
```bash
python stress_test_analyzer.py
```

## 📊 Скрипты тестирования

### `load_test_with_profiling.py`

**Базовый скрипт нагрузочного тестирования с профилированием**

**Конфигурация:**
```python
BASE_URL = "http://localhost:5000"  # URL сервера
TEST_DURATION = 30                   # Длительность теста (сек)
NUM_WORKERS = 5                      # Количество параллельных воркеров
RPS_PER_WORKER = 10                  # RPS на воркер
```

**Тестируемые эндпоинты:**
- `POST /clicks` - Создание кликов
- `POST /events/track` - Трекинг событий
- `POST /conversions/track` - Трекинг конверсий
- `GET /v1/campaigns` - Получение кампаний

**Вывод:**
- Статистика запросов (RPS, время отклика, ошибки)
- Системные ресурсы (CPU, память)
- Профилирование Python кода

### `stress_test_analyzer.py`

**Расширенный анализатор стресс-тестирования**

**Сценарии тестирования:**
- `light_load`: 2 воркера × 5 RPS = 10 RPS total
- `medium_load`: 5 воркеров × 10 RPS = 50 RPS total
- `heavy_load`: 10 воркеров × 20 RPS = 200 RPS total
- `extreme_load`: 20 воркеров × 50 RPS = 1000 RPS total

**Функции:**
- Автоматическое сравнение сценариев
- Графическая визуализация результатов
- Анализ трендов производительности
- Рекомендации по оптимизации

## 📈 Анализ результатов

### Метрики производительности

**Основные показатели:**
- **RPS (Requests Per Second)** - Пропускная способность
- **Response Time** - Время отклика (avg, p95, p99)
- **Success Rate** - Процент успешных запросов
- **CPU/Memory Usage** - Использование системных ресурсов

### Оценка производительности

| Метрика | Отлично | Хорошо | Приемлемо | Плохо |
|---------|---------|--------|-----------|-------|
| Success Rate | ≥99% | ≥95% | ≥90% | <90% |
| P95 Response | ≤0.5s | ≤1.0s | ≤2.0s | >2.0s |
| RPS | ≥1000 | ≥500 | ≥100 | <100 |

## 🔬 Профилирование

### Инструменты профилирования

1. **cProfile** - Стандартный профилировщик Python
2. **PyCharm Profiler** - Встроенный профилировщик IDE
3. **analyze_profile.py** - Анализатор профилировочных данных

### Анализ узких мест

**Типичные bottlenecks:**
- **I/O операции** (93% времени в наших тестах)
- **База данных** - медленные запросы
- **CPU** - тяжелые вычисления
- **Память** - утечки или неэффективное использование

### Запуск профилирования

```bash
# Запуск с профилированием
python -m cProfile -o profile_output.pstat load_test_with_profiling.py

# Анализ результатов
python analyze_profile.py
```

## 🚀 Рекомендации по оптимизации

### На основе анализа профилирования

#### 1. I/O Оптимизации (самый большой эффект)
```python
# Кеширование результатов БД
from cachetools import TTLCache
cache = TTLCache(maxsize=1000, ttl=300)  # 5 минут

# Асинхронные операции
async def get_campaign(campaign_id):
    if campaign_id in cache:
        return cache[campaign_id]

    # БД запрос
    result = await db.fetch_one("SELECT * FROM campaigns WHERE id = $1", campaign_id)
    cache[campaign_id] = result
    return result
```

#### 2. Оптимизация запросов
```sql
-- Добавление индексов
CREATE INDEX idx_events_campaign_created ON events(campaign_id, created_at);
CREATE INDEX idx_clicks_campaign_ip ON clicks(campaign_id, ip_address);

-- Оптимизация запросов
SELECT * FROM events
WHERE campaign_id = $1 AND created_at >= $2
ORDER BY created_at DESC
LIMIT 100;
```

#### 3. Connection Pooling
```python
# Увеличение размера пула для высокой нагрузки
pool = psycopg2.pool.SimpleConnectionPool(
    minconn=5,      # Минимум соединений
    maxconn=50,     # Максимум соединений (увеличено)
    host="localhost",
    database="supreme_octosuccotash_db",
    user="app_user",
    password="app_password"
)
```

#### 4. Кеширование на уровне приложения
```python
# Redis для часто запрашиваемых данных
import redis

redis_client = redis.Redis(host='localhost', port=6379, db=0)

def get_campaign_analytics(campaign_id):
    cache_key = f"campaign_analytics:{campaign_id}"

    # Проверяем кеш
    cached = redis_client.get(cache_key)
    if cached:
        return json.loads(cached)

    # Вычисляем аналитику
    analytics = calculate_campaign_analytics(campaign_id)

    # Кешируем на 5 минут
    redis_client.setex(cache_key, 300, json.dumps(analytics))

    return analytics
```

### Масштабирование

#### Горизонтальное масштабирование
1. **Load Balancer** (nginx/haproxy)
2. **Несколько инстансов приложения**
3. **Read Replicas** для базы данных
4. **Redis Cluster** для кеширования

#### Вертикальное масштабирование
1. **Увеличение CPU cores**
2. **Больше RAM** для кеширования
3. **SSD диски** для БД
4. **Network optimization**

## 📊 Мониторинг производительности

### Ключевые метрики для отслеживания

```python
# Добавьте в приложение
from prometheus_client import Counter, Histogram, Gauge

# Метрики
REQUEST_COUNT = Counter('http_requests_total', 'Total HTTP requests', ['method', 'endpoint', 'status'])
REQUEST_LATENCY = Histogram('http_request_duration_seconds', 'HTTP request latency', ['method', 'endpoint'])
ACTIVE_CONNECTIONS = Gauge('active_db_connections', 'Active database connections')
CACHE_HIT_RATE = Gauge('cache_hit_rate', 'Cache hit rate percentage')
```

### Система мониторинга
- **Prometheus** + **Grafana** для визуализации
- **Alert Manager** для оповещений
- **APM инструменты** (New Relic, DataDog)

## 🎯 Лучшие практики

### 1. Регулярное тестирование
```bash
# Добавьте в CI/CD pipeline
python stress_test_analyzer.py --scenario=medium_load --alert-threshold=95
```

### 2. Постепенное увеличение нагрузки
- Начинайте с низкой нагрузки
- Постепенно увеличивайте RPS
- Мониторьте точки насыщения

### 3. A/B тестирование оптимизаций
- Тестируйте оптимизации на staging
- Сравнивайте метрики до/после
- Откатывайте неудачные изменения

### 4. Профилирование в production
```python
# Выборочное профилирование медленных запросов
if response_time > 1.0:  # секунды
    profiler.enable()
    # код обработки
    profiler.disable()
    # отправить профиль в систему анализа
```

## 📞 Поддержка

При обнаружении проблем с производительностью:

1. **Запустите профилирование**: `python analyze_profile.py`
2. **Проверьте системные ресурсы**: `python load_test_with_profiling.py`
3. **Запустите стресс-тест**: `python stress_test_analyzer.py`
4. **Проанализируйте логи** сервера на ошибки
5. **Проверьте базу данных** на медленные запросы

---

*Этот набор инструментов обеспечивает комплексный анализ производительности вашего API и помогает принимать обоснованные решения по оптимизации.* 🚀
