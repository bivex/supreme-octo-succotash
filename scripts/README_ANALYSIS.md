# Анализ Админпанели

Этот набор скриптов позволяет автоматически анализировать качество кода админпанели с помощью инструмента Designite (DPy).

## 📋 Доступные скрипты

### 1. `analyze_admin_panel.sh` - Запуск анализа

**Описание**: Выполняет полный анализ админпанели и организует результаты по типам.

**Что делает**:
- Создает папку с текущей датой в `analysis_results/YYYY-MM-DD/`
- Организует результаты в структурированные подпапки:
  - `architecture_smells/` - Архитектурные проблемы
  - `design_smells/` - Проблемы проектирования
  - `implementation_smells/` - Проблемы реализации
  - `metrics/` - Метрики кода
  - `logs/` - Логи анализа

**Использование**:
```bash
./scripts/analyze_admin_panel.sh
```

### 2. `view_analysis_results.sh` - Просмотр результатов

**Описание**: Показывает сводку по последнему анализу в удобном формате.

**Что показывает**:
- Количество найденных проблем по категориям
- Пути к файлам результатов
- Общую статистику
- Инструкции для детального просмотра

**Использование**:
```bash
./scripts/view_analysis_results.sh
```

### 3. `list_analyses.sh` - История анализов

**Описание**: Показывает историю всех проведенных анализов с временными метками.

**Что показывает**:
- Список всех уникальных анализов (сортировка по времени)
- Количество проблем в каждом анализе
- Маркер 🆕 для самого свежего анализа
- Пути к папкам результатов

**Использование**:
```bash
./scripts/list_analyses.sh
```

## 📊 Структура результатов

```
analysis_results/
└── 2025-12-14/                    # Дата анализа
    ├── 2025-12-14_13-00-22/       # Уникальный анализ с временем
    │   ├── architecture_smells/
    │   │   └── admin_panel_arch_smells.json
    │   ├── design_smells/
    │   │   └── admin_panel_design_smells.json
    │   ├── implementation_smells/
    │   │   └── admin_panel_implementation_smells.json
    │   ├── metrics/
    │   │   ├── admin_panel_class_module_metrics.json
    │   │   └── admin_panel_function_metrics.json
    │   └── logs/                  # Если есть логи
    └── 2025-12-14_09-15-30/       # Предыдущий анализ
        └── ...                    # Та же структура
```

## 🔍 Типы обнаруживаемых проблем

### Архитектурные проблемы (Architecture Smells)
- **Feature Concentration**: Компоненты реализуют слишком много разнородных функций
- **God Component**: Компоненты с чрезмерной ответственностью
- **Unstable Dependency**: Нестабильные зависимости

### Проблемы проектирования (Design Smells)
- **Deficient Encapsulation**: Нарушение инкапсуляции
- **Multifaceted Abstraction**: Классы с множественными ответственностями
- **Insufficient Modularization**: Недостаточная модульность
- **Feature Envy**: Методы зависят от данных других классов

### Проблемы реализации (Implementation Smells)
- **Long Statement**: Слишком длинные строки кода (>120 символов)
- **Magic Number**: Использование "магических" чисел без констант
- **Empty Catch Block**: Пустые блоки обработки исключений
- **Complex Method**: Методы с высокой цикломатической сложностью
- **Long Method**: Слишком длинные методы

## 📈 Метрики кода

- **Метрики классов и модулей**: Размер, сложность, связность
- **Метрики функций**: Цикломатическая сложность, размер, параметры

## 🛠️ Детальный просмотр результатов

Для детального анализа используйте `jq`:

```bash
# Просмотр архитектурных проблем
cat analysis_results/2025-12-14/architecture_smells/admin_panel_arch_smells.json | jq .

# Поиск конкретных проблем
cat analysis_results/2025-12-14/implementation_smells/admin_panel_implementation_smells.json | jq '.[] | select(.Smell == "Long statement")'

# Подсчет проблем по типам
cat analysis_results/2025-12-14/implementation_smells/admin_panel_implementation_smells.json | jq '.[] | .Smell' | sort | uniq -c
```

## 📅 Автоматизация и множественные анализы

### Уникальность анализов
Каждый запуск анализа создает уникальную папку с временной меткой, поэтому вы можете:
- Запускать анализ несколько раз в день
- Сравнивать результаты разных запусков
- Отслеживать прогресс исправления проблем

### Просмотр истории анализов
```bash
# Посмотреть все доступные анализы
find analysis_results -mindepth 2 -maxdepth 2 -type d -name "*_*" | sort -r

# Сравнить два последних анализа
LATEST=$(find analysis_results -mindepth 2 -maxdepth 2 -type d -name "*_*" | sort -r | head -1)
PREVIOUS=$(find analysis_results -mindepth 2 -maxdepth 2 -type d -name "*_*" | sort -r | head -2 | tail -1)

echo "Сравнение: $LATEST vs $PREVIOUS"
```

### Cron автоматизация
Добавьте в cron для регулярного анализа:

```bash
# Еженедельный анализ по понедельникам в 9:00
0 9 * * 1 /path/to/project/scripts/analyze_admin_panel.sh

# Ежедневный анализ в рабочее время
0 10 * * 1-5 /path/to/project/scripts/analyze_admin_panel.sh
```

## 🎯 Рекомендации по исправлению

1. **Архитектурные проблемы**: Разделить компоненты по принципу единственной ответственности
2. **Проблемы проектирования**: Исправить инкапсуляцию и модульность
3. **Проблемы реализации**: Соблюдать PEP8, заменить магические числа константами
4. **Пустые catch-блоки**: Добавить логирование или корректную обработку

## 📝 Логи

Результаты каждого анализа сохраняются в папке с датой, что позволяет отслеживать прогресс исправления проблем со временем.